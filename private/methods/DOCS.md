# Methods
This directory serves as script location for helper functions and etc for functions that are repurposed for more than one specific endpoint.

Such clear example is: [utils](./utilsMethods.js), which provides multiple functions that provides support and utility for all endpoints and functions for specific shared endpoints.